package de.benshu.commons.core;

/**
 * Provides a {@code toString}-equivalent method with the unambiguous purpose of rendering the object for debugging.
 */
public interface Debuggable {
    String debug();
}
